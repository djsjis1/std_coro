# TSan suppressions for known data races
# These are pre-existing issues that need proper fixes

# io_uring ring access from thread pool threads
# The thread pool accesses the event loop's io_uring ring concurrently
# TODO: Fix by giving each thread its own ring or adding proper synchronization
race:_io_uring_get_sqe
race:io_uring_get_sqe
race:io_uring_submit
race:io_uring_peek_batch_cqe
race:io_uring_wait_cqe_timeout

# EventLoop::wake() vs EventLoop::~EventLoop() race
# Scheduler destructor calls stop() -> wake() on worker threads' event loops
# while the worker thread is destroying its thread-local EventLoop
# TODO: Fix by ensuring worker threads exit before Scheduler destroys
race:coro::EventLoop::wake
race:coro::EventLoop::~EventLoop
race:coro::Scheduler::~Scheduler
